 Промпт / ТЗ для AI-разработчика
Роль: Ты — Senior Full-Stack Architect и Lead Developer. Твоя задача — спроектировать и разработать ядро CreativeCMS.
Контекст:
CreativeGroup — это технологический конгломерат, состоящий из 7 компаний (CreativeStudio, CreativeTech, CreativeMedia, CreativeGaming, YourSite, LogiFlex, JobHunt). Нам нужна единая Headless CMS для управления контентом всех этих проектов. Это не просто блог-движок, а Enterprise-платформа с визуальным конструктором страниц.
Стек технологий (строгие требования):
Frontend: Next.js 14+ (App Router), React 18, TypeScript.
State Management: Zustand (для глобального стейта) + TanStack Query (для серверного стейта).
UI Framework: TailwindCSS + Shadcn/UI (Radix Primitives).
Drag & Drop: @dnd-kit/core (для конструктора страниц).
Forms: React Hook Form + Zod.
Rich Text: TipTap Editor.
Icons: Lucide React.
Database (Simulation/Real): Prisma Schema (как референс данных), но в текущей итерации используем Mock Data / LocalStorage для полной фронтенд-симуляции без бэкенда.
🏛 Архитектура и Основные Модули
1. Мультисайтовость (Multi-tenancy)
CMS должна поддерживать переключение между "Воркспейсами" (Workspaces).
Логика: В сайдбаре есть переключатель проектов (Studio, Tech, Media, etc.).
Изоляция: При переключении на CreativeMedia пользователь видит только статьи и новости. При переключении на LogiFlex — документацию API и кейсы. При переключении на YourSite — шаблоны сайтов.
Цветовое кодирование: Каждый воркспейс имеет свой акцентный цвет (Studio - черный, Tech - синий, Media - желтый и т.д.).
2. Visual Page Builder (Конструктор страниц)
Самая важная часть. Нужен аналог Notion или Webflow (упрощенно).
Структура: Слева — библиотека блоков. По центру — Canvas. Справа — Инспектор свойств.
Блоки: Реализуй следующие блоки как JSON-компоненты:
Hero Section (Заголовок, подзаголовок, картинка, кнопки).
Grid/Columns (Контейнер для других блоков).
Feature List (Иконка + текст).
Rich Text (TipTap редактор).
CTA Block.
Функционал:
Drag-and-Drop: перетаскивание блоков из библиотеки в Canvas.
Reordering: изменение порядка блоков внутри Canvas.
Nested Blocks: возможность вкладывать блоки друг в друга (например, Текст внутри Колонок).
Props Editing: При клике на блок справа открывается форма для редактирования его свойств (текст, цвет, отступы).
3. Dynamic Content Engine (Гибкие коллекции)
Аналог Strapi.
Интерфейс для создания типов контента (например, создать коллекцию "Турниры" для CreativeGaming).
Табличный вид (Data Grid) для просмотра записей с фильтрацией и сортировкой.
4. Media Library
Сетка изображений.
Drag-and-drop зона для загрузки.
Возможность выбрать изображение и вставить его в Page Builder.
5. AI Assistant Integration
В текстовых полях и Page Builder должна быть кнопка "✨ AI Optimize".
Она должна (симулировать) переписывание текста, генерацию SEO-тегов и исправление ошибок.
🎨 Дизайн-система (UI/UX)
Стиль: "Apple Professional".
Фон: Чистый белый (#FFFFFF) или светло-серый (#F5F5F7).
Типографика: Inter или SF Pro. Четкая иерархия, плотный трекинг.
Тени: Мягкие, рассеянные (shadow-sm, shadow-xl для модалок).
Бордеры: Тонкие, едва заметные (border-gray-100).
Анимации: Плавные переходы при переключении табов и открытии панелей (framer-motion).
📝 Задачи для реализации (Step-by-Step)
Создать Layout:
Реализовать CMSLayout с боковой панелью (Collapsible Sidebar) и Topbar.
Внедрить WorkspaceSwitcher с выпадающим списком компаний.
Реализовать Dashboard:
Виджеты статистики (используй recharts).
Лента активности (Activity Feed).
Статус сервисов (интеграция с данными Status Page).
Разработать Page Editor (Ядро):
Создать хук usePageEditor для управления деревом JSON-блоков.
Настроить @dnd-kit для вертикальной сортировки.
Сверстать компоненты-рендеры (Renderer) для каждого типа блока.
Связать данные:
Использовать constants.tsx как базу данных. CMS должна уметь читать и "обновлять" (в стейте) данные сайта.
💡 Критерии приемки
Код должен быть модульным (отдельно компоненты блоков, отдельно логика редактора).
Интерфейс не должен выглядеть как "Bootstrap админка", он должен выглядеть как продукт.
Drag-and-drop должен работать плавно, с анимацией.
Начни с реализации архитектуры Page Editor и компонентов блоков.